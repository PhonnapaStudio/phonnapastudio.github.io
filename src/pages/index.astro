---
import Nav from "../components/Nav.astro";
import Hero from "../components/Hero.astro";
import Highlight from "../components/Highlight.astro";
import Atmosphere from "../components/Atmosphere.astro";
import Promotion from "../components/Promotion.astro";
import Review from "../components/Review.astro";
import Contact from "../components/Contact.astro";
import Footer from "../components/Footer.astro";
import Service from "../components/Service.astro";
import "../styles/global.css";
---

<!doctype html>
<html lang="th">
  <head>
    <!-- Charset & Viewport -->
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <!-- Title & Description -->
    <title>ร้านทำเล็บคลองหก คลอง 6 ใกล้ราชมงคลธัญบุรี | Phonnapa Studio</title>
    <meta
      name="description"
      content="Phonnapa Studio ร้านทำเล็บคลองหก คลอง 6 ใกล้สะพานชมพู ม.ราชมงคลธัญบุรี บริการทำเล็บเจล ต่อเล็บ PVC เพ้นท์เล็บ สปามือเท้า ขนตา ราคานักศึกษา ค้นหาร้านทำเล็บใกล้ฉัน เจอเราได้เลย!"
    />
    <meta
      name="keywords"
      content="ร้านทำเล็บคลองหก, ร้านทำเล็บคลอง 6, ทำเล็บใกล้ราชมงคลธัญบุรี, ร้านทำเล็บใกล้ฉัน, เล็บเจลคลองหก, คลอง 6, ร้านทำเล็บสะพานชมพู, ร้านเพ้นท์เล็บราคานักศึกษา, สะพานชมพู, มหาวิทยาลัยราชมงคลธัญบุรี, ทำเล็บใกล้มหาลัย"
    />
    <meta name="author" content="Phonnapa Studio" />
    <meta name="application-name" content="Phonnapa Studio" />
    <meta name="robots" content="index, follow" />
    <link rel="canonical" href="https://phonnapastudio.github.io/" />

    <!-- Google Site Verification -->
    <meta
      name="google-site-verification"
      content="w60tMNbkFtNA_XxIlE1bvUxK2uz-3dqiG_3JbI13wpI"
    />

    <!-- Open Graph / Facebook -->
    <meta property="og:type" content="website" />
    <meta property="og:url" content="https://phonnapastudio.github.io/" />
    <meta
      property="og:title"
      content="ร้านทำเล็บคลองหก คลอง 6 ใกล้ราชมงคลธัญบุรี | Phonnapa Studio"
    />
    <meta
      property="og:description"
      content="ร้านทำเล็บคลองหก คลอง 6 ใกล้สะพานชมพู – เดินทางง่ายจากราชมงคลธัญบุรี ทำเล็บราคานักศึกษา เปิดทุกวัน!"
    />
    <meta
      property="og:image"
      content="https://phonnapastudio.github.io/images/cover.webp"
    />
    <meta property="og:site_name" content="Phonnapa Studio" />

    <!-- Twitter Card -->
    <meta name="twitter:card" content="summary_large_image" />
    <meta
      name="twitter:title"
      content="ร้านทำเล็บคลองหก คลอง 6 ใกล้ราชมงคลธัญบุรี | Phonnapa Studio"
    />
    <meta
      name="twitter:description"
      content="ใกล้มหาลัย ใกล้ฉัน ทำเล็บเจล ต่อเล็บ เพ้นท์เล็บ PVC – เหมาะกับนักศึกษา เดินทางสะดวก สะพานชมพู คลอง 6"
    />
    <meta
      name="twitter:image"
      content="https://phonnapastudio.github.io/images/cover.webp"
    />

    <!-- Sitemap -->
    <link rel="sitemap" href="/sitemap-index.xml" />

    <!-- Favicon -->
    <link rel="icon" type="image/png" href="/images/logo.webp" />

    <!-- Fonts & Libraries -->

    <link
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css"
      rel="stylesheet"
    />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Sarabun:wght@300;400;500;600;700&display=swap"
      rel="stylesheet"
    />

    <style is:global>
      html {
        scroll-behavior: smooth;
      }
      :root {
        --color-primary: #f78cbc;
        --color-primary-light: #e8d9f1;
        --color-accent: #ffadc3;
        --color-accent-light: #fbcff4;
        --color-bg: #fff9fb;
        --color-bg-soft: #f5f0fa;
      }

      body {
        font-family: "Sarabun", sans-serif;
        background-color: var(--color-bg);
      }

      .bg-primary {
        background-color: var(--color-primary);
      }
      .bg-primary-light {
        background-color: var(--color-primary-light);
      }
      .bg-accent {
        background-color: var(--color-accent);
      }
      .bg-accent-light {
        background-color: var(--color-accent-light);
      }
      .bg-soft {
        background-color: var(--color-bg-soft);
      }

      .text-primary {
        color: var(--color-primary);
      }
      .text-accent {
        color: var(--color-accent);
      }

      .border-primary {
        border-color: var(--color-primary);
      }
      .border-accent {
        border-color: var(--color-accent);
      }

      .hover-primary:hover {
        background-color: var(--color-primary);
      }
      .hover-accent:hover {
        background-color: var(--color-accent);
      }

      .gradient-bg {
        background: linear-gradient(
          135deg,
          var(--color-bg) 0%,
          var(--color-bg-soft) 100%
        );
      }

      .hero-gradient {
        background: linear-gradient(
          135deg,
          var(--color-primary-light) 0%,
          var(--color-accent-light) 100%
        );
      }

      .card-hover {
        transition: all 0.3s ease;
      }

      .card-hover:hover {
        transform: translateY(-5px);
        box-shadow: 0 20px 40px rgba(247, 140, 188, 0.15);
      }

      .star-animation {
        animation: twinkle 1.5s ease-in-out infinite alternate;
      }

      @keyframes twinkle {
        0% {
          opacity: 0.7;
        }
        100% {
          opacity: 1;
        }
      }

      .bounce-slow {
        animation: bounce 2s infinite;
      }

      .fade-in-up {
        animation: fadeInUp 0.8s ease-out;
      }

      @keyframes fadeInUp {
        from {
          opacity: 0;
          transform: translateY(30px);
        }
        to {
          opacity: 1;
          transform: translateY(0);
        }
      }

      .hero-image {
        background: linear-gradient(
          45deg,
          rgba(247, 140, 188, 0.1),
          rgba(255, 173, 195, 0.1)
        );
        backdrop-filter: blur(10px);
      }

      @keyframes pulse-slow {
        0%,
        100% {
          opacity: 0.3;
          transform: scale(1);
        }
        50% {
          opacity: 0.5;
          transform: scale(1.1);
        }
      }

      @keyframes pulse-slower {
        0%,
        100% {
          opacity: 0.2;
          transform: scale(0.95);
        }
        50% {
          opacity: 0.35;
          transform: scale(1.05);
        }
      }

      .animate-pulse-slow {
        animation: pulse-slow 6s ease-in-out infinite;
      }

      .animate-pulse-slower {
        animation: pulse-slower 10s ease-in-out infinite;
      }
    </style>
  </head>

  <body class="gradient-bg">
    <Nav />

    <Hero />

    <Service />

    <Atmosphere />

    <Highlight />

    <Promotion />

    <Review />

    <Contact />

    <Footer />

    <button
      id="scrollToTop"
      class="fixed bottom-8 right-2 bg-primary hover:bg-opacity-80 text-white p-3 rounded-full shadow-lg transition-all duration-300 opacity-0 invisible z-20"
    >
      <i class="fas fa-arrow-up"></i>
    </button>

    <!-- Instagram Floating Button (อยู่เหนือปุ่มไลน์) -->
    <a
      href="https://instagram.com/phonnapa_studio"
      target="_blank"
      rel="noopener"
      id="igButton"
      class="fixed bottom-36
      right-1 bg-gradient-to-tr from-[#F58529] via-[#DD2A7B] to-[#8134AF]
      hover:opacity-90 text-white p-3 rounded-full shadow-lg transition-all
      duration-300 z-20"
      aria-label="ดูรูปใน Instagram"
    >
      <i class="fab fa-instagram text-xl"></i>
    </a>

    <a
      href="https://line.me/R/ti/p/@153nuymp"
      target="_blank"
      rel="noopener"
      id="lineButton"
      class="fixed bottom-22 right-1 bg-[#00B900] hover:bg-opacity-90 text-white p-3 rounded-full shadow-lg transition-all duration-300 z-20"
      aria-label="แชทผ่านไลน์"
    >
      <i class="fab fa-line text-xl"></i>
    </a>

    <!-- Schema.org JSON-LD -->
    <script type="application/ld+json">
      {
        "@context": "https://schema.org",
        "@type": "NailSalon",
        "name": "Phonnapa Studio",
        "image": "https://phonnapastudio.github.io/images/cover.webp",
        "url": "https://phonnapastudio.github.io/",
        "description": "ร้านทำเล็บคลองหก ใกล้ราชมงคลธัญบุรี – ช่างมืออาชีพ ราคานักศึกษา เพ้นท์เล็บ ต่อเล็บ PVC ขนตา",
        "priceRange": "฿฿",
        "telephone": "+66 95 665 8550",
        "address": {
          "@type": "PostalAddress",
          "addressLocality": "คลองหลวง",
          "addressRegion": "ปทุมธานี",
          "postalCode": "12110",
          "addressCountry": "TH"
        },
        "openingHoursSpecification": [
          {
            "@type": "OpeningHoursSpecification",
            "dayOfWeek": [
              "Monday",
              "Tuesday",
              "Wednesday",
              "Thursday",
              "Friday",
              "Saturday",
              "Sunday"
            ],
            "opens": "09:00",
            "closes": "21:00"
          }
        ]
      }
    </script>

    <!-- Main Script -->
    <script>
      document.addEventListener("DOMContentLoaded", () => {
        if (window.innerWidth >= 768) {
          const link = document.createElement("link");
          link.rel = "stylesheet";
          link.href =
            "https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css";
          document.head.appendChild(link);
        }
        // --------------------------
        // Navigation active highlight
        const sections = document.querySelectorAll("section[id]");
        const navLinks = document.querySelectorAll(".nav-link");
        const navObserver = new IntersectionObserver(
          (entries) => {
            entries.forEach((entry) => {
              const id = entry.target.getAttribute("id");
              const navLink = document.querySelector(
                `.nav-link[href="#${id}"]`
              );
              if (entry.isIntersecting && navLink) {
                navLinks.forEach((link) =>
                  link.classList.remove(
                    "text-primary",
                    "font-semibold",
                    "underline"
                  )
                );
                navLink.classList.add(
                  "text-primary",
                  "font-semibold",
                  "underline"
                );
              }
            });
          },
          { threshold: 0.3 }
        );
        sections.forEach((section) => navObserver.observe(section));

        // --------------------------
        // Animate on Scroll (skip on mobile)
        const isMobile = window.innerWidth < 768;
        const animatedEls = document.querySelectorAll("[data-animate]");

        if (isMobile) {
          animatedEls.forEach((el) => el.classList.remove("opacity-0"));
        } else {
          const animateObserver = new IntersectionObserver(
            (entries, observer) => {
              entries.forEach((entry) => {
                if (entry.isIntersecting) {
                  const el = entry.target;
                  const animation =
                    (el as HTMLElement).dataset.animate || "fadeInUp";
                  el.classList.add(
                    "animate__animated",
                    `animate__${animation}`
                  );
                  el.classList.remove("opacity-0");
                  observer.unobserve(el);
                }
              });
            },
            { threshold: 0.25 }
          );
          animatedEls.forEach((el) => animateObserver.observe(el));
        }

        // --------------------------
        // Mobile menu toggle
        const toggleBtn = document.getElementById("menu-toggle");
        const menu = document.getElementById("mobile-menu");
        if (toggleBtn && menu) {
          toggleBtn.addEventListener("click", () => {
            menu.classList.toggle("hidden");
          });
        }

        // --------------------------
        // Scroll to Top Button
        const scrollToTopBtn = document.getElementById("scrollToTop");
        if (scrollToTopBtn) {
          window.addEventListener("scroll", () => {
            scrollToTopBtn.classList.toggle(
              "opacity-0",
              window.pageYOffset <= 300
            );
            scrollToTopBtn.classList.toggle(
              "invisible",
              window.pageYOffset <= 300
            );
          });

          scrollToTopBtn.addEventListener("click", () => {
            window.scrollTo({ top: 0, behavior: "smooth" });
          });
        }

        // --------------------------
        // Smooth anchor scroll
        document.querySelectorAll('a[href^="#"]').forEach((anchor) => {
          anchor.addEventListener("click", (e) => {
            const targetId = anchor.getAttribute("href");
            if (!targetId) return;

            const target = document.querySelector(targetId);
            if (target instanceof HTMLElement) {
              e.preventDefault();
              target.scrollIntoView({ behavior: "smooth", block: "start" });
            }
          });
        });
      });
    </script>
  </body>
</html>
